<Page
    x:Class="Netcool.DbToys.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Netcool.DbToys.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d" NavigationCacheMode="Required">
    <ScrollViewer>
        <controls:MarkdownTextBlock x:Name="MarkdownText"
                                    Margin="6"
                                    Header1Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                                    Text="{x:Bind ViewModel.Message, Mode=TwoWay}"
                                   />
        <!--
        <TextBlock  Text="{x:Bind ViewModel.Message, Mode=TwoWay}"
            TextWrapping="Wrap" MinWidth="400" />
            -->
    </ScrollViewer>


</Page>
